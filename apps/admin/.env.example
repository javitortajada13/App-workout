# Same values as apps/mobile/.env.example -- public/non-secret (safe to
# ship in a client bundle): the API's public URL and Supabase's public
# project URL/publishable key.
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
